/** Return the `k`-th smallest (0-index) element in `arr`.
 *
 * 🚀 linear time
 *
 * linear space
 *
 * Constant space is possible: https://doi.org/10.1007/3-540-19487-8_2
 */
export declare function bfprt(arr: number[], k: number): number;
export declare function partition(arr: number[], pivot: number): {
    small: number[];
    same: number[];
    big: number[];
};
/** Return the median of `arr`.
 * But if `arr.length` is even, return the larger middle element.
 *
 * 🚀 linear time
 *
 * linear space
 */
export declare function median(arr: number[]): number;
//# sourceMappingURL=bfprt.d.ts.map