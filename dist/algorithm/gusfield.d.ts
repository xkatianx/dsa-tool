/** Gusfield's algorithm / Z algorithm
 *
 * `return[i]` is the maximum `k` such that `str[0..k] == str[i..i+k]`,
 * except that `return[0] == 0`
 *
 * 🚀 linear time
 *
 * 🤏 linear space
 */
export declare function gusfield(str: string): number[];
//# sourceMappingURL=gusfield.d.ts.map